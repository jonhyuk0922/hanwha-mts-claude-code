# 워크시트 — 한화투자증권 MTS 개발자 교육 (A3 양면)

이름: ______________   짝: ______________   2026-09-22(월)

숫자 칸의 단위는 세 가지만 쓴다. **사용률(%, 차이는 %p) · 왕복(회) · 분.** 다른 단위는 적지 않는다.
사용률은 `/context` 첫 줄의 숫자, 왕복은 내가 보낸 프롬프트 수, 분은 시계로 잰 값이다.

---

# 앞면

## 1교시 — 토큰 경제와 세션 운영

조사 과제(고정): "주문 수량 검증 로직이 어느 파일 어느 함수에 흩어져 있는지 파일:함수 목록으로 내라."
A: 폴더 파일을 열어 읽기 / B: `rg` → `ast-grep` → 서브에이전트. B 전에 반드시 `/clear`. 판정선: ④ 가 15%p 이상.

| 칸 | 값 |
|---|---|
| ① 시작 사용률 (`/clear` 직후) | ______ % |
| ② A 종료 사용률 | ______ % |
| ③ B 종료 사용률 | ______ % |
| ④ 하락 (② − ③) | ______ %p |
| ⑤ `/usage` 오늘 누적 | ______ % |

통과: ④ ≥ 15 → [ ] O   [ ] X

## 2교시 — 검증 우선 프롬프팅

로직 결함 ①② 는 실패 테스트를 먼저 쓴다. 화면 결함 ③ 은 기준 스크린샷을 붙여넣고 비교한다.
판정은 "실패 재현 → 수정 후 통과" 두 단계가 다 보여야 O.

| 결함 | 실행 명령 | 출력 (핵심 한 줄) | 판정 | 사용률 |
|---|---|---|---|---|
| ① applyQuote | `npm test -- tests/applyQuote.race.test.ts` | | [ ] 실패 재현 [ ] 통과 | ______ % |
| ② validateQty | `npm test -- tests/validateQty.boundary.test.ts` | | [ ] 실패 재현 [ ] 통과 | ______ % |
| ③ Watchlist 화면 | 화면 비교 프롬프트 (기준 스크린샷 첨부) | 차이 ______ 건 | [ ] 차이 검출 [ ] 일치 | ______ % |

## 점심 직전

`/usage` 잔량 ______ % — 절반 이상 소모했으면 오후 자체 실습은 짝과 한 계정으로 페어.

## 3교시 — 계획 분리와 세션 인계

| 값 | 사용률 |
|---|---|
| 계획 세션 종료 직전 `/context` | ______ % |
| 새 세션에서 SPEC.md 만 읽힌 직후 `/context` | ______ % |

짝 판정 3칸: 새 세션 첫 응답 질문 0건 [ ]  /  3파일 경로 명시 [ ]  /  초과 파일 1개 이하 [ ]
X 가 있으면 SPEC.md 네 칸(대상 파일 · 인터페이스 · 범위 밖 · E2E 검증 단계) 중 빈 칸: ______________

## 4교시 — 팀 표준을 레포에

| 값 | 사용률 |
|---|---|
| 프루닝 전 — 초안 180줄을 둔 채 새 세션 `/context` | ______ % |
| 프루닝 후 — 정본 55줄을 둔 채 새 세션 `/context` | ______ % |

내 정본 줄 수: ______ 줄 (목표 55, 상한 200)
드리프트 재검증(짝과 비교): 데이터 페칭 위치 [ ] 같음  /  네이밍 [ ] 같음  /  로딩·에러 처리 [ ] 같음
근거 줄 (CLAUDE.md 줄 번호): ______ / ______ / ______      훅 커밋 [ ] `.claude/settings.json`

## 5교시 — 코드 리뷰와 자동 반영

숫자는 뒷면 지표 ①② 에 적는다. 여기서는 체크만.
자동 리뷰 코멘트 도착 [ ]  /  Important 1건 지목 [ ]  /  Nit 분리 [ ]  /  브랜치 직접 편집 없이 체크 green [ ]

## 6교시 — 병렬 작업과 팀 루프

태스크당 비용 — 대체 단위 3종. 서식: `[관심종목 정렬 추가] 컨텍스트 +38%p · 왕복 6회 · 22분`
세션 시작 직후와 종료 직전에 `/context` 를 한 번씩 친다. 증가분 = 종료 − 시작.

| 태스크명 | 컨텍스트 증가 | 왕복 | 소요 |
|---|---|---|---|
| [ ______________________ ] | + ______ %p | ______ 회 | ______ 분 |

시작 직후 `/context` ______ %  →  종료 직전 ______ %

한 줄로: `[ ______________ ] 컨텍스트 + ____ %p · 왕복 ____ 회 · ____ 분`

---

# 뒷면

## 지표 5종

오늘 실측 3개(①②③)는 값을 적는다. 4주 뒤 2개(④⑤)는 값 대신 **측정 원천 / 담당자 / 첫 집계일** 을 적는다.

| # | 지표 | 시점 | 오늘 값 | 측정 원천 | 담당자 | 첫 집계일 |
|---|---|---|---|---|---|---|
| ① | 첫 리뷰까지 걸린 시간 | 오늘 | ______ 분 | PR open → 자동 리뷰 코멘트 도착 | ________ | ____/____ |
| ② | 첫 구현 통과율 | 오늘 | [ ] O  [ ] X | 첫 푸시에서 CI green 여부 (본인 1건) | ________ | ____/____ |
| ③ | 완료 태스크당 비용 | 오늘 | + ____ %p · ____ 회 · ____ 분 | 대체 단위 3종 (앞면 6교시) | ________ | ____/____ |
| ④ | 사람이 브랜치를 건드리지 않고 해결된 코멘트 비율 | 4주 뒤 | — | PR 메타데이터 누적 | ________ | ____/____ |
| ⑤ | 머지 전 잡힌 결함 대 프로덕션 유출 | 4주 뒤 | — | 인시던트 트래커 | ________ | ____/____ |

리뷰당 $15~25 같은 금액 지표는 관리형 서비스와 API 과금에만 해당한다. 개인 Pro/Max 에서는 대체 단위 3종으로 기준선을 잡는다.

## 4주 계획표

| 주차 | 할 일 | 대상 레포 | 담당자 | 마감일 | 완료 확인 방법 |
|---|---|---|---|---|---|
| 1주차 | CLAUDE.md · 훅(`.claude/settings.json`) 체크인 | ________ | ________ | ____/____ | main 에 커밋 존재 |
| 2주차 | REVIEW.md 와 자동 리뷰를 실제 레포 1곳에 | ________ | ________ | ____/____ | 자동 코멘트 달린 PR 1건 |
| 3주차 | 지표 ①②③ 수집 시작 | ________ | ________ | ____/____ | 주간 기록 1회 |
| 4주차 | ④⑤ 원천 연결 · 첫 회고 | ________ | ________ | ____/____ | 회고 기록 |

오늘 정한 것 중 회사에 돌아가서 첫 번째로 할 일 한 줄: ______________________________________________
